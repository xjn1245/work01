# 历史辅助模块说明

这些模块已从项目根目录迁入 `livetalking/experimental/`，根目录保留了兼容入口文件：

- `domain_knowledge.py`：留学领域语义理解样例模块
- `multimodal_parser.py`：多模态输入解析样例模块
- `real_time_optimization.py`：实时优化实验模块

目的：
- 降低根目录文件密度
- 保持现有导入路径不破坏

